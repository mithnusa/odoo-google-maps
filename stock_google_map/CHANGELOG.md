# Change Log

## 19.0.1.0.2

### Improved

- **Python — Black format** (`models/stock_picking.py`): Reformatted `fields.Float` and `fields.Char` definitions to multi-line style.
- **XML format** (`views/stock_picking.xml`): Self-closing tags, multi-line attribute layout for `<google_map>` and `<field>` elements.
- **Manifest**: Website URL updated to `https://www.mithnusa.com`; version normalized to `19.0.1.0.2`; removed `installable`, `application`, `auto_install` keys.

## 19.0.1.0.1

### Improved

- **Manifest**: Rewrote summary and description; fixed module category from `'Hidden'` to `'Inventory'`; removed empty `demo` key

## 19.0.1.0.0
Migration to version 19.0
