# Change log

## 17.0.1.1.0
- New google_map sub-view `google_map_crm`. This new sub-view will allow us to update the information shown on marker info window and the sidebar.
- Added an overlay to the marker, which will show a brief information of the marker when hovered.